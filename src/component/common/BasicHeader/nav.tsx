import React from 'react';

function Nav() {
  const [value, setValue] = React.useState(0);

  function handleChange(event, newValue) {
    setValue(newValue);
  }

  return <div>dsds</div>;
}

export default Nav;
